/**
 * Projects feature API configuration.
 *
 * Re-exports shared base URL helpers for project CRUD,
 * activity feeds, and project-scoped sub-resource API calls.
 */

export { defaultApiBaseUrl, normalizeApiBaseUrl } from "@/shared/api/base";
