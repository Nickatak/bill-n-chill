/**
 * Cost-codes feature API configuration.
 *
 * Re-exports shared base URL helpers for cost-code CRUD and
 * lookup API calls used across estimating and budgets.
 */

export { defaultApiBaseUrl, normalizeApiBaseUrl } from "@/shared/api/base";
